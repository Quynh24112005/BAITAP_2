{
  "name": "baitap2",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
